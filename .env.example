# Base de dados Supabase (PostgreSQL)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Segredo para assinar os JWT (gerar um valor aleatório longo e seguro)
JWT_SECRET=substitui_por_um_segredo_longo_e_aleatorio

# Ambiente
NODE_ENV=development

# Porta do servidor local
PORT=3000
